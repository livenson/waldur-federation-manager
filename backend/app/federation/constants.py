"""OpenID Federation 1.0 constants."""

# Media types
ENTITY_STATEMENT_JWT = "application/entity-statement+jwt"
RESOLVE_RESPONSE_JWT = "application/resolve-response+jwt"
TRUST_MARK_JWT = "application/trust-mark+jwt"

# Entity types
OPENID_RELYING_PARTY = "openid_relying_party"
OPENID_PROVIDER = "openid_provider"
OAUTH_AUTHORIZATION_SERVER = "oauth_authorization_server"
OAUTH_CLIENT = "oauth_client"
OAUTH_RESOURCE = "oauth_resource"
FEDERATION_ENTITY = "federation_entity"

# Well-known path
WELL_KNOWN_PATH = "/.well-known/openid-federation"
